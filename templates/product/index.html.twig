
{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} BD {% endblock %}


{% block body %}
    <style>
        body {
        }
        h1{
            text-align: center;
        }

        a{
            display: block;
            background-color: #F5F5F5;
            color: black;
            padding: 25px;
            text-align: center;
            text-decoration: none;
            margin-left: auto;
            margin-right: auto;
            margin: 5px;
        }
        a:hover{
            background-color: #e0e0e0;
        }

    </style>

    <h1>Products</h1>

    <a href = "{{ path('add_product') }}" >Add product</a></li>

    <a href = "{{ path('listProducts') }}" >List products</a>

    <a href = "{{ path('index') }}" >Return</a>

{% endblock %}

